# Server Configuration
PORT=3000
NODE_ENV=development

# Base URL (used for email links - must be absolute URLs)
# In production: https://yourdomain.com
# In development: http://localhost:3000 (defaults to this if not set)
BASE_URL=http://localhost:3000

# Supabase Configuration (Backend - Service Role)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_SERVICE_ANON_KEY=your-anon-key

# Resend Email Configuration
RESEND_API_KEY=re_your_api_key
FROM_EMAIL=invites@yourdomain.com

# Frontend Supabase Configuration (Client-side - Build-time variables)
# These MUST be provided as build arguments during Docker build
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
BASE_URL=http://localhost:3000